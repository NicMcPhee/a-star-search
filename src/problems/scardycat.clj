(ns hanoi)

(defrecord State [[tower-start] [tower-mid] [tower-finish]])

(defn legal? [tower]
  (and ))

(defn move [space]
  (let [xx ()
        yy ()]
    (assoc-in (assoc-in space_)
                        )))

(defn children [state]
  (let